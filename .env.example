# Example environment variables for local development and for documenting required Render secrets
# DO NOT commit real secrets. Use this file as a template only.

# Database (Postgres)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/homelink_db
SPRING_DATASOURCE_USERNAME=homelink_user
SPRING_DATASOURCE_PASSWORD=supersecret

# JWT secret used by JwtService
JWT_SECRET=change-me-to-a-long-random-string

# Optional: override server port for local testing
SERVER_PORT=8080
